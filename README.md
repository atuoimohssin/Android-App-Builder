# MyAutoBuilderApp

## Project Structure
This is an auto-generated Android project template with GitHub Actions CI/CD.

## Setup Instructions
1. Replace gradlew and gradlew.bat with actual executable files from an Android Studio project
2. Add gradle-wrapper.jar to gradle/wrapper/ directory
3. Push to GitHub to trigger automatic APK builds

## GitHub Actions
The workflow will automatically build a debug APK on every push to main/master branch.